@* @page "/market"
@using BankApp.Data.Entities.Auth
@using BankApp.Data.Entities.Market
@using BankApp.Data.Entities.Banking
@using BankApp.Services
@using Microsoft.EntityFrameworkCore
@using BlazorApp.Components.Elements
@rendermode InteractiveServer

@inject IdentityService IdentityService
@inject StockMarketService MarketService
@inject IDbContextFactory<BankApp.Data.BankAppDbContext> DbFactory
@inject AppNavigator Nav
@implements IDisposable

<div class="container-fluid py-4 bg-black min-vh-100">
    @if (availableStocks == null)
    {
        <div class="d-flex justify-content-center align-items-center" style="height: 80vh;">
            <div class="spinner-grow text-danger" role="status"></div>
        </div>
    }
    else
    {
        <div class="row g-4">
            <div class="col-xl-3 col-lg-4">
                <div class="glass-panel p-0 overflow-hidden h-100">
                    <div class="p-3 border-bottom border-secondary border-opacity-10 bg-black">
                        <div class="d-flex justify-content-between align-items-center mb-3">
                            <h6 class="text-white fw-bold small text-uppercase m-0">Market Assets</h6>
                            <span class="badge bg-danger pulse-red">LIVE</span>
                        </div>
                        <div class="search-container">
                            <input type="text" @bind="searchTerm" @bind:event="oninput"
                                   class="form-control search-input" placeholder="Search Assets..." />
                        </div>
                    </div>

                    <div class="list-group list-group-flush custom-scroll" style="max-height: 70vh; overflow-y: auto;">
                        @foreach (var stock in FilteredStocks)
                        {
                            var changePercent = ((stock.CurrentPrice % 1.5m) - 0.75m);
                            var isPositive = changePercent >= 0;

                            <button @onclick="() => SelectStock(stock)"
                                    class="stock-card @(selectedStock?.Id == stock.Id ? "active" : "")">
                                <div class="d-flex justify-content-between align-items-center w-100">
                                    <div>
                                        <div class="ticker-text">@stock.Ticker</div>
                                        <div class="text-secondary tiny-caps">@stock.Name</div>
                                    </div>
                                    <div class="text-end">
                                        <div class="fw-bold text-white">@FormatCurrency(GetConvertedPrice(stock.CurrentPrice))</div>
                                        <div class="small @(isPositive ? "text-success" : "text-danger")">
                                            @(isPositive ? "▲" : "▼") @Math.Abs(changePercent).ToString("P2")
                                        </div>
                                    </div>
                                </div>
                            </button>
                        }
                    </div>
                </div>
            </div>

            <div class="col-xl-6 col-lg-8">
                <div class="glass-panel p-4 mb-4">
                    <div class="d-flex justify-content-between align-items-start mb-4">
                        <div>
                            <h1 class="display-6 fw-black text-white mb-0">@selectedStock?.Ticker</h1>
                            <p class="text-secondary small fw-bold">REAL-TIME MARKET DATA</p>
                        </div>
                        <div class="text-end">
                            <div class="text-secondary tiny-caps fw-bold">@(hoverPrice > 0 ? "SELECTED PRICE" : "LAST PRICE")</div>
                            <h2 class="fw-black @(hoverPrice > 0 ? "text-info" : "text-white") m-0 tracking-tighter">
                                @FormatCurrency(GetConvertedPrice(hoverPrice > 0 ? hoverPrice : (selectedStock?.CurrentPrice ?? 0)))
                            </h2>
                        </div>
                    </div>

                    <div class="graph-wrapper border border-secondary border-opacity-10 bg-black" style="height: 450px; position: relative;" @onmouseleave="() => hoverPrice = 0">
                        @if (candles.Any())
                        {
                            <svg viewBox="0 0 1000 450" preserveAspectRatio="none" style="width: 100%; height: 100%;">
                                @for (int i = 0; i <= 5; i++)
                                {
                                    var y = i * 90;
                                    <line x1="0" y1="@y" x2="1000" y2="@y" stroke="#1a1a1a" stroke-width="1" />
                                }

                                @foreach (var c in candles)
                                {
                                    <rect x="@(c.X - 8)" y="@(450 - (c.VolumeHeight * 0.8f))" width="16" height="@(c.VolumeHeight * 0.8f)" fill="@(c.IsUp ? "#19875422" : "#dc354522")" />
                                }

                                <polyline fill="none" stroke="#0dcaf0" stroke-width="1.5" points="@GetTrendPoints()" stroke-linejoin="round" opacity="0.3" />

                                @foreach (var c in candles)
                                {
                                    <line x1="@c.X" y1="@c.HighY" x2="@c.X" y2="@c.LowY" stroke="@(c.IsUp ? "#198754" : "#dc3545")" stroke-width="1" />
                                    <rect x="@(c.X - 5)" y="@Math.Min(c.OpenY, c.CloseY)" width="10" height="@Math.Max(2, Math.Abs(c.OpenY - c.CloseY))" fill="@(c.IsUp ? "#198754" : "#dc3545")" />

                                    @if (c.TradeType != null)
                                    {
                                        <g>
                                            <rect x="@(c.X - 25)" y="@(c.HighY - 35)" width="50" height="18" rx="2" fill="@(c.TradeType == "BUY" ? "#198754" : "#dc3545")" />
                                            <text x="@c.X" y="@(c.HighY - 22)" fill="white" font-size="10" font-weight="900" text-anchor="middle">@c.TradeType</text>
                                        </g>
                                    }
                                }

                                @foreach (var c in candles)
                                {
                                    <rect x="@(c.X - 10)" y="0" width="20" height="450" fill="transparent" class="hover-area" @onmouseover="() => hoverPrice = c.Price" />
                                    @if (hoverPrice == c.Price)
                                    {
                                        <line x1="0" y1="@c.CloseY" x2="1000" y2="@c.CloseY" stroke="#0dcaf0" stroke-width="1" stroke-dasharray="4" opacity="0.6" />
                                        <circle cx="@c.X" cy="@c.CloseY" r="4" fill="#0dcaf0" />
                                    }
                                }
                            </svg>
                        }
                    </div>
                </div>
            </div>

            <div class="col-xl-3">
                <div class="glass-panel p-4 mb-4 position-relative overflow-hidden" style="min-height: 300px;">
                    @if (user == null)
                    {
                        <div class="position-absolute top-0 start-0 w-100 h-100 d-flex flex-column align-items-center justify-content-center bg-black bg-opacity-50" style="z-index: 10; backdrop-filter: blur(2px);">
                            <i class="bi bi-shield-lock text-danger mb-3" style="font-size: 2rem;"></i>
                            <h5 class="fw-bold text-white text-center px-4">PURCHASE PANEL<br />RESTRICTED</h5>
                            <button class="btn btn-outline-danger rounded-pill fw-bold px-4 mt-3 tiny-caps" @onclick='() => Nav.NavigateTo("/login")'>Login to Purchase</button>
                        </div>
                    }

                    <h6 class="text-white fw-bold small text-uppercase mb-4 border-bottom border-secondary border-opacity-25 pb-2">Order Execution</h6>
                    <div class="mb-3">
                        <label class="text-secondary tiny-caps mb-2 d-flex justify-content-between align-items-end">
                            <span>Order Quantity</span>
                            <div class="text-end">
                                <div class="text-white">HOLD: @ownedQuantity.ToString("N2")</div>
                                <div class="text-danger fw-bold" style="font-size: 10px;">
                                    TOTAL: @FormatCurrency(GetConvertedPrice(selectedStock?.CurrentPrice ?? 0) * orderQuantity)
                                </div>
                            </div>
                        </label>
                        <input type="number" step="0.01" @bind="orderQuantity" @bind:event="oninput" class="form-control trade-input" disabled="@(user == null)" />
                    </div>

                    <div class="row g-2">
                        <div class="col-6"><button @onclick="() => HandleTrade(true)" class="btn btn-success w-100 fw-bold rounded-1 py-3" disabled="@(user == null)">BUY</button></div>
                        <div class="col-6"><button @onclick="() => HandleTrade(false)" class="btn btn-danger w-100 fw-bold rounded-1 py-3" disabled="@(user == null)">SELL</button></div>
                        <div class="col-6"><button @onclick="ExecuteBuyMax" class="btn btn-dark border-secondary w-100 fw-bold rounded-1 py-2 tiny-caps mt-1" disabled="@(user == null)">BUY MAX</button></div>
                        <div class="col-6"><button @onclick="ExecuteSellAll" class="btn btn-dark border-secondary w-100 fw-bold rounded-1 py-2 tiny-caps mt-1" disabled="@(user == null)">SELL ALL</button></div>
                    </div>
                </div>

                @if (user?.BankAccount?.CurrencyType != null)
                {
                    <div class="glass-panel p-3 border-danger border-start border-4">
                        <small class="text-secondary tiny-caps d-block">Available Funds</small>
                        <h4 class="text-white fw-bold m-0">@FormatCurrency(user.BankAccount.Balance)</h4>
                    </div>
                }
            </div>
        </div>
    }
</div>

<style>
    .glass-panel {
        background: #0c0c0c;
        border: 1px solid #1f1f1f;
    }

    .fw-black {
        font-weight: 900;
    }

    .ticker-text {
        font-weight: 800;
        color: white;
        font-size: 1.1rem;
    }

    .search-input {
        background: #000;
        border: 1px solid #222;
        color: white;
        border-radius: 4px;
        font-size: 0.8rem;
    }

        .search-input:focus {
            background: #050505;
            border-color: #dc3545;
            color: white;
            box-shadow: none;
        }

    .stock-card {
        background: transparent;
        border: none;
        width: 100%;
        text-align: left;
        padding: 15px 20px;
        border-bottom: 1px solid #111;
        transition: all 0.2s ease;
    }

        .stock-card:hover {
            background: #151515;
        }

        .stock-card.active {
            background: #1a1a1a;
            border-left: 4px solid #dc3545;
        }

    .trade-input {
        background: #000;
        border: 1px solid #333;
        color: white;
        font-weight: 700;
        border-radius: 4px;
    }

    .pulse-red {
        animation: pulseR 2s infinite;
    }
    @@keyframes pulseR {
        0%, 100% {
            opacity: 1;
        }

        50% {
            opacity: 0.4;
        }
    }

    .tiny-caps {
        font-size: 10px;
        text-transform: uppercase;
        letter-spacing: 1px;
    }

    .custom-scroll::-webkit-scrollbar {
        width: 4px;
    }

    .custom-scroll::-webkit-scrollbar-thumb {
        background: #333;
        border-radius: 10px;
    }
</style>

@code {
    [CascadingParameter] public Toast? Toast { get; set; }

    private ApplicationUser? user;
    private List<Stock>? availableStocks;
    private Stock? selectedStock;
    private decimal orderQuantity = 1;
    private decimal ownedQuantity = 0;
    private decimal hoverPrice = 0;
    private string searchTerm = "";
    private System.Threading.Timer? _timer;
    private List<CandleModel> candles = new();
    private Dictionary<long, string> tradeHistoryMarkers = new();

    private IEnumerable<Stock> FilteredStocks => (availableStocks ?? new()).Where(s => string.IsNullOrEmpty(searchTerm) || s.Ticker.Contains(searchTerm, StringComparison.OrdinalIgnoreCase));

    protected override async Task OnInitializedAsync()
    {
        user = await IdentityService.GetCurrentUserAsync();
        await RefreshMarketData();
        if (availableStocks?.Any() == true) selectedStock = availableStocks.First();
        await BuildGraph();

        _timer = new System.Threading.Timer(async _ =>
        {
            await InvokeAsync(async () =>
            {
                await RefreshMarketData();
                await BuildGraph();
                StateHasChanged();
            });
        }, null, 1000, 1000);
    }

    private async Task RefreshMarketData()
    {
        using var db = await DbFactory.CreateDbContextAsync();
        availableStocks = await db.Stocks.AsNoTracking().ToListAsync();

        if (selectedStock != null)
        {
            selectedStock = availableStocks.FirstOrDefault(s => s.Id == selectedStock.Id);
            if (user?.BankAccount != null)
            {
                var inv = await db.Investments.AsNoTracking()
                    .FirstOrDefaultAsync(i => i.BankAccountId == user.BankAccount.Id && i.StockId == selectedStock.Id);
                ownedQuantity = inv?.Quantity ?? 0;
            }
        }
    }

    private string FormatCurrency(decimal amount) => $"{(user?.BankAccount?.CurrencyType?.CurrencySymbol ?? "$")}{amount:N2}";

    private async Task BuildGraph()
    {
        if (selectedStock == null) return;
        using var db = await DbFactory.CreateDbContextAsync();
        var history = await db.StockHistory.AsNoTracking()
            .Where(h => h.StockId == selectedStock.Id)
            .OrderByDescending(h => h.Timestamp).Take(40).OrderBy(h => h.Timestamp).ToListAsync();

        if (history.Count < 2) return;

        decimal minPrice = history.Min(h => h.Price);
        decimal maxPrice = history.Max(h => h.Price);
        decimal range = (maxPrice - minPrice) == 0 ? 1 : (maxPrice - minPrice) * 1.5m;
        decimal baseline = minPrice - (range * 0.2m);

        candles.Clear();
        float step = 1000f / history.Count;
        Random rng = new();

        for (int i = 0; i < history.Count; i++)
        {
            var current = history[i].Price;
            var prev = i == 0 ? current : history[i - 1].Price;
            decimal vol = range * 0.05m;
            candles.Add(new CandleModel
            {
                Id = history[i].Id,
                X = i * step,
                OpenY = Map(prev, baseline, range),
                CloseY = Map(current, baseline, range),
                HighY = Map(Math.Max(current, prev) + (decimal)rng.NextDouble() * vol, baseline, range),
                LowY = Map(Math.Min(current, prev) - (decimal)rng.NextDouble() * vol, baseline, range),
                IsUp = current >= prev,
                Price = current,
                VolumeHeight = rng.Next(20, 60),
                TradeType = tradeHistoryMarkers.GetValueOrDefault(history[i].Id)
            });
        }
    }

    private float Map(decimal val, decimal min, decimal range) => 350 - (float)((val - min) / range * 300);
    private string GetTrendPoints() => string.Join(" ", candles.Select(c => $"{c.X},{c.CloseY}"));

    private async Task HandleTrade(bool isBuy)
    {
        if (user == null || selectedStock == null || orderQuantity <= 0) return;

        var (success, msg) = await MarketService.ExecuteTrade(user.Id, selectedStock.Id, orderQuantity, isBuy);

        if (success)
        {
            Toast?.Show(msg, Toast.ToastLevel.Success);
            using var db = await DbFactory.CreateDbContextAsync();
            var last = await db.StockHistory.Where(h => h.StockId == selectedStock.Id).OrderByDescending(h => h.Timestamp).FirstOrDefaultAsync();
            if (last != null) tradeHistoryMarkers[last.Id] = isBuy ? "BUY" : "SELL";

            user = await IdentityService.GetCurrentUserAsync(); // Refresh balance
            await RefreshMarketData();
            await BuildGraph();
        }
        else { Toast?.Show(msg, Toast.ToastLevel.Error); }
    }

    private async Task ExecuteBuyMax()
    {
        if (selectedStock != null && user?.BankAccount != null)
        {
            decimal p = GetConvertedPrice(selectedStock.CurrentPrice);
            orderQuantity = p > 0 ? Math.Floor((user.BankAccount.Balance / p) * 100m) / 100m : 0;
            if (orderQuantity > 0) await HandleTrade(true);
        }
    }

    private async Task ExecuteSellAll() { orderQuantity = ownedQuantity; if (orderQuantity > 0) await HandleTrade(false); }
    private decimal GetConvertedPrice(decimal p) => user?.BankAccount?.CurrencyType == null ? p : p * user.BankAccount.CurrencyType.Rate;
    public void Dispose() => _timer?.Dispose();
    private async Task SelectStock(Stock stock) { selectedStock = stock; tradeHistoryMarkers.Clear(); await BuildGraph(); }

    public class CandleModel
    {
        public long Id; public float X, OpenY, CloseY, HighY, LowY, VolumeHeight;
        public bool IsUp; public decimal Price; public string? TradeType;
    }
} *@